const Rooms = ({ bullet }) => {
    return <section className="rooms section-2"
    id="espacios"
    style={{
        display: bullet === 2 ? "flex" : "none"
    }}
>
    </section>
}

export default Rooms;